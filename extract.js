const fs = require('fs'); const pdf = require('pdf-parse'); let dataBuffer = fs.readFileSync('public/varun-sinha-resume.pdf'); (pdf.default ? pdf.default(dataBuffer) : pdf(dataBuffer)).then(data => { fs.writeFileSync('resume_text.txt', data.text); console.log('Extracted'); });
